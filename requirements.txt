# NumPy 2.x: MediaPipe’s import chain may load TensorFlow/JAX; JAX expects np.dtypes.StringDType (NumPy 2+).
# Use a venv (`python -m venv .venv`) so a clean stack matches these pins.
opencv-python==4.10.0.84
numpy==2.1.3
mediapipe==0.10.8
streamlit==1.43.2
flask==3.0.2
imageio-ffmpeg==0.6.0
